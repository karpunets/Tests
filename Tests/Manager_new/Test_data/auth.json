{
  "request": {
    "principal": "$principal",
    "credential": "$credential"
  },
  "schema": {
    "name": "X-Smiddle-Auth-Token",
    "token": {"type":"string"},
    "sessionLiveTimeSec": "$sessionLiveTimeSec"
  }
}